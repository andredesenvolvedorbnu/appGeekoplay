'use client';

import Link from 'next/link';
import { useEffect, useMemo, useRef, useState } from 'react';
import { Copy, Flag, Gamepad2, Heart, IdCard, Image as ImageIcon, Loader2, MessageCircle, MoreHorizontal, Rocket, Send, Share2, X } from 'lucide-react';
import { createClient } from '@/lib/supabase/client';

const categories=['Todos','Anime','Games','HQs & Comics','Filmes','Séries','Cosplay','Mangá','K-Pop','RPG','Tecnologia','Colecionáveis'];

type Profile={id:string;display_name:string;username:string|null;avatar_url:string|null;level:number;xp:number};
type CardData={title?:string;subtitle?:string;rarity?:string;atk?:number;def?:number;description?:string;category?:string;photo_url?:string};
type Post={id:string;author_id:string;content:string|null;image_url:string|null;category:string|null;post_type:string;card_data:CardData|null;is_boosted:boolean;boosted_until:string|null;created_at:string};
type Comment={id:string;post_id:string;author_id:string;content:string;created_at:string};

function GeekCardPost({post,author}:{post:Post;author?:Profile}){
 const card=post.card_data||{};
 return <div className="px-3 pb-4 sm:px-4">
  <div className="mx-auto w-full max-w-[360px] overflow-hidden rounded-[26px] border-2 border-orange-400/55 bg-gradient-to-b from-[#28202f] to-[#101218] shadow-xl">
   <div className="flex items-center justify-between gap-3 border-b border-white/10 px-4 py-3"><div className="min-w-0"><p className="text-[10px] font-black uppercase tracking-[.18em] text-orange-300">{card.rarity||'Raro'}</p><h3 className="truncate font-black">{card.title||author?.display_name||'Geek Card'}</h3></div><IdCard className="shrink-0 text-orange-300" size={20}/></div>
   <div className="mx-3 mt-3 aspect-[3/4] overflow-hidden rounded-2xl border border-white/10 bg-black/35 flex items-center justify-center">{(card.photo_url||post.image_url)?<img src={card.photo_url||post.image_url||''} alt="Foto do Geek Card" className="h-full w-full object-cover object-center"/>:<div className="text-xs text-slate-500">Sem foto</div>}</div>
   <div className="p-4"><div className="flex items-center justify-between gap-2"><span className="rounded-full bg-orange-500/15 px-2 py-1 text-[10px] text-orange-300">{card.category||post.category||'Geek'}</span><span className="text-xs text-slate-400">Lv.{author?.level||1} · {author?.xp||0} XP</span></div>{card.subtitle&&<p className="mt-3 text-sm font-bold text-orange-100">{card.subtitle}</p>}<p className="mt-2 min-h-10 text-xs leading-5 text-slate-300">{card.description||post.content||'Meu Geek Card no GeekoPlay.'}</p><div className="mt-4 grid grid-cols-2 gap-2"><div className="rounded-xl bg-red-500/10 p-3 text-center"><span className="text-[10px] text-red-300">ATK</span><p className="text-xl font-black">{card.atk??0}</p></div><div className="rounded-xl bg-cyan-500/10 p-3 text-center"><span className="text-[10px] text-cyan-300">DEF</span><p className="text-xl font-black">{card.def??0}</p></div></div><div className="mt-4 border-t border-white/10 pt-3 text-[10px] text-slate-500">@{author?.username||'geek'} · GeekoPlay</div></div>
  </div>
 </div>
}

export function FeedClient(){
 const supabase=useMemo(()=>createClient(),[]); const fileInputRef=useRef<HTMLInputElement>(null);
 const [userId,setUserId]=useState<string|null>(null); const [profiles,setProfiles]=useState<Record<string,Profile>>({}); const [posts,setPosts]=useState<Post[]>([]); const [likes,setLikes]=useState<Record<string,number>>({}); const [liked,setLiked]=useState<Set<string>>(new Set()); const [commentCounts,setCommentCounts]=useState<Record<string,number>>({}); const [comments,setComments]=useState<Record<string,Comment[]>>({}); const [openComments,setOpenComments]=useState<Set<string>>(new Set()); const [commentDrafts,setCommentDrafts]=useState<Record<string,string>>({}); const [content,setContent]=useState(''); const [category,setCategory]=useState('Games'); const [filter,setFilter]=useState('Todos'); const [loading,setLoading]=useState(true); const [sending,setSending]=useState(false); const [uploading,setUploading]=useState(false); const [selectedFile,setSelectedFile]=useState<File|null>(null); const [previewUrl,setPreviewUrl]=useState<string|null>(null); const [message,setMessage]=useState(''); const [openMenu,setOpenMenu]=useState<string|null>(null);

 async function reload(){
  setLoading(true); const {data:{user}}=await supabase.auth.getUser(); setUserId(user?.id||null);
  const {data:postRows}=await supabase.from('posts').select('id,author_id,content,image_url,category,post_type,card_data,is_boosted,boosted_until,created_at').order('created_at',{ascending:false}).limit(60);
  const safe=(postRows||[]) as Post[]; setPosts(safe); const ids=[...new Set(safe.map(p=>p.author_id))];
  let profileMap:Record<string,Profile>={}; if(ids.length){const {data}=await supabase.from('profiles').select('id,display_name,username,avatar_url,level,xp').in('id',ids);(data||[]).forEach((p:Profile)=>profileMap[p.id]=p)}
  if(safe.length){const postIds=safe.map(p=>p.id); const [{data:likeRows},{data:commentRows}]=await Promise.all([supabase.from('likes').select('post_id,user_id').in('post_id',postIds),supabase.from('comments').select('id,post_id,author_id,content,created_at').in('post_id',postIds).order('created_at',{ascending:true})]); const counts:Record<string,number>={}; const mine=new Set<string>(); (likeRows||[]).forEach((l:{post_id:string;user_id:string})=>{counts[l.post_id]=(counts[l.post_id]||0)+1;if(user&&l.user_id===user.id)mine.add(l.post_id)}); setLikes(counts);setLiked(mine); const grouped:Record<string,Comment[]>={}; const cCounts:Record<string,number>={}; const commentAuthorIds=new Set<string>(); (commentRows||[]).forEach((c:Comment)=>{(grouped[c.post_id]||=[]).push(c);cCounts[c.post_id]=(cCounts[c.post_id]||0)+1;commentAuthorIds.add(c.author_id)}); const missing=[...commentAuthorIds].filter(id=>!profileMap[id]); if(missing.length){const {data}=await supabase.from('profiles').select('id,display_name,username,avatar_url,level,xp').in('id',missing);(data||[]).forEach((p:Profile)=>profileMap[p.id]=p)} setComments(grouped);setCommentCounts(cCounts);
  }else{setLikes({});setLiked(new Set());setComments({});setCommentCounts({})}
  setProfiles(profileMap); setLoading(false);
 }
 useEffect(()=>{void reload();const channel=supabase.channel('feed-live-v2').on('postgres_changes',{event:'*',schema:'public',table:'posts'},()=>void reload()).on('postgres_changes',{event:'*',schema:'public',table:'comments'},()=>void reload()).on('postgres_changes',{event:'*',schema:'public',table:'likes'},()=>void reload()).on('postgres_changes',{event:'UPDATE',schema:'public',table:'profiles'},()=>void reload()).subscribe();return()=>{void supabase.removeChannel(channel)}},[supabase]);

 function chooseFile(file:File|null){setMessage('');if(!file){if(previewUrl)URL.revokeObjectURL(previewUrl);setSelectedFile(null);setPreviewUrl(null);return}if(!['image/jpeg','image/png','image/webp'].includes(file.type)){setMessage('Envie uma imagem JPG, PNG ou WEBP.');return}if(file.size>10*1024*1024){setMessage('A imagem precisa ter no máximo 10 MB.');return}if(previewUrl)URL.revokeObjectURL(previewUrl);setSelectedFile(file);setPreviewUrl(URL.createObjectURL(file))}
 async function uploadImage(){if(!selectedFile||!userId)return null;setUploading(true);const ext=selectedFile.name.split('.').pop()?.toLowerCase()||'jpg';const path=`${userId}/${crypto.randomUUID()}.${ext}`;const {error}=await supabase.storage.from('posts').upload(path,selectedFile,{contentType:selectedFile.type,cacheControl:'3600',upsert:false});if(error)throw error;const {data}=supabase.storage.from('posts').getPublicUrl(path);return data.publicUrl}
 async function createPost(){if(!userId||(!content.trim()&&!selectedFile))return;setSending(true);setMessage('');try{const imageUrl=selectedFile?await uploadImage():null;const {error}=await supabase.from('posts').insert({author_id:userId,content:content.trim()||null,image_url:imageUrl,category,post_type:'post'});if(error)throw error;setContent('');chooseFile(null);if(fileInputRef.current)fileInputRef.current.value='';await reload()}catch{setMessage('Não foi possível publicar. Verifique o conteúdo e tente novamente.')}finally{setSending(false);setUploading(false)}}
 async function toggleLike(postId:string){if(!userId)return;if(liked.has(postId))await supabase.from('likes').delete().eq('user_id',userId).eq('post_id',postId);else await supabase.from('likes').insert({user_id:userId,post_id:postId});await reload()}
 function toggleComments(postId:string){setOpenComments(current=>{const next=new Set(current);next.has(postId)?next.delete(postId):next.add(postId);return next})}
 async function sendComment(postId:string){const text=(commentDrafts[postId]||'').trim();if(!userId||!text)return;const {error}=await supabase.from('comments').insert({post_id:postId,author_id:userId,content:text});if(error){setMessage('Não foi possível enviar o comentário.');return}setCommentDrafts(c=>({...c,[postId]:''}));await reload()}
 function postUrl(id:string){return typeof window==='undefined'?'':`${window.location.origin}/?post=${id}`}
 async function copyPost(id:string){try{await navigator.clipboard.writeText(postUrl(id));setMessage('Link da publicação copiado.')}catch{setMessage('Não foi possível copiar o link.')}setOpenMenu(null)}
 async function sharePost(post:Post){const url=postUrl(post.id);if(navigator.share){try{await navigator.share({title:'GeekoPlay',text:post.post_type==='card'?'Veja este Geek Card no GeekoPlay':post.content?.slice(0,120)||'Veja esta publicação no GeekoPlay',url})}catch{}}else await copyPost(post.id);setOpenMenu(null)}
 async function reportPost(id:string){if(!userId)return;if(!window.confirm('Deseja denunciar esta publicação para análise da equipe GeekoPlay?'))return setOpenMenu(null);const {error}=await supabase.from('content_reports').insert({reporter_id:userId,content_type:'post',content_id:id,reason:'conteúdo denunciado pelo usuário'});if(error?.code==='23505')setMessage('Você já denunciou esta publicação.');else if(error)setMessage('Não foi possível enviar a denúncia.');else setMessage('Denúncia enviada para análise.');setOpenMenu(null)}

 const now=Date.now(); const visible=(filter==='Todos'?posts:posts.filter(p=>p.category===filter)).slice().sort((a,b)=>{const ab=a.is_boosted&&(!a.boosted_until||new Date(a.boosted_until).getTime()>now);const bb=b.is_boosted&&(!b.boosted_until||new Date(b.boosted_until).getTime()>now);if(ab!==bb)return ab?-1:1;return new Date(b.created_at).getTime()-new Date(a.created_at).getTime()});
 return <div className="mx-auto max-w-2xl space-y-4 px-3 sm:px-4">
  <section className="rounded-2xl border border-geek-line bg-geek-panel p-4"><textarea value={content} onChange={e=>setContent(e.target.value)} maxLength={5000} placeholder="No que você está pensando, geek? 🎮" className="min-h-24 w-full resize-none rounded-xl border border-geek-line bg-geek-soft p-3 outline-none focus:border-geek-orange"/>{previewUrl&&<div className="relative mt-3 flex max-h-[460px] min-h-32 items-center justify-center overflow-hidden rounded-xl border border-geek-line bg-black/30"><button onClick={()=>chooseFile(null)} className="absolute right-2 top-2 z-10 rounded-full bg-black/70 p-1.5"><X size={16}/></button><img src={previewUrl} alt="Prévia da imagem" className="block h-auto max-h-[460px] w-auto max-w-full object-contain"/></div>}<input ref={fileInputRef} type="file" accept="image/jpeg,image/png,image/webp" hidden onChange={e=>chooseFile(e.target.files?.[0]||null)}/><div className="mt-3 flex flex-wrap items-center gap-2"><button onClick={()=>fileInputRef.current?.click()} className="flex items-center gap-2 rounded-xl border border-geek-line bg-geek-soft px-3 py-2 text-xs"><ImageIcon size={15}/>Foto</button><Link href="/meu-card" className="flex items-center gap-2 rounded-xl border border-geek-line bg-geek-soft px-3 py-2 text-xs"><IdCard size={15}/>Meu Card</Link><select value={category} onChange={e=>setCategory(e.target.value)} className="ml-auto rounded-xl border border-geek-line bg-geek-soft px-3 py-2 text-xs">{categories.slice(1).map(c=><option key={c}>{c}</option>)}</select><button onClick={createPost} disabled={sending||uploading||(!content.trim()&&!selectedFile)} className="rounded-xl bg-geek-orange px-4 py-2 text-sm font-bold disabled:opacity-50">{uploading?'Enviando foto...':sending?'Publicando...':'Publicar'}</button></div>{message&&<p className="mt-2 text-xs text-slate-300">{message}</p>}</section>
  <div className="overflow-x-auto pb-1"><div className="flex min-w-max gap-2">{categories.map(c=><button key={c} onClick={()=>setFilter(c)} className={`rounded-full border px-3 py-2 text-xs ${filter===c?'border-geek-orange bg-geek-orange text-white':'border-geek-line bg-geek-panel text-slate-300'}`}>{c}</button>)}</div></div>
  {loading?<div className="grid place-items-center py-16 text-slate-400"><Loader2 className="animate-spin"/></div>:visible.length===0?<div className="rounded-2xl border border-dashed border-geek-line bg-geek-panel p-10 text-center text-slate-400"><Gamepad2 className="mx-auto mb-3 text-geek-orange"/>Ainda não há publicações nesta categoria.</div>:visible.map(post=>{const author=profiles[post.author_id];const postComments=comments[post.id]||[];const boosted=post.is_boosted&&(!post.boosted_until||new Date(post.boosted_until).getTime()>Date.now());return <article key={post.id} className={`overflow-visible rounded-2xl border bg-geek-panel ${boosted?'border-orange-500/50':'border-geek-line'}`}>{boosted&&<div className="flex items-center gap-2 border-b border-orange-500/20 bg-orange-500/5 px-4 py-2 text-[11px] font-bold text-orange-300"><Rocket size={14}/>Publicação impulsionada</div>}<div className="relative flex items-center gap-3 p-4"><Link href={`/perfil/${post.author_id}`} className="h-10 w-10 shrink-0 overflow-hidden rounded-full bg-gradient-to-br from-orange-400 to-purple-600">{author?.avatar_url&&<img src={author.avatar_url} alt="" className="h-full w-full object-cover object-center"/>}</Link><div className="min-w-0"><Link href={`/perfil/${post.author_id}`} className="block truncate text-sm font-bold hover:text-orange-300">{author?.display_name||'Geek'}</Link><p className="text-[11px] text-slate-500">Lv.{author?.level||1} · {new Date(post.created_at).toLocaleString('pt-BR')}</p></div>{post.category&&<span className="ml-auto hidden rounded-full bg-orange-500/10 px-2 py-1 text-[10px] text-orange-300 sm:inline">{post.category}</span>}<button onClick={()=>setOpenMenu(openMenu===post.id?null:post.id)} className="rounded-lg p-2 text-slate-400 hover:bg-geek-soft" aria-label="Mais opções"><MoreHorizontal size={18}/></button>{openMenu===post.id&&<div className="absolute right-4 top-14 z-30 w-52 overflow-hidden rounded-xl border border-geek-line bg-[#171b22] shadow-2xl"><button onClick={()=>copyPost(post.id)} className="flex w-full items-center gap-2 px-4 py-3 text-left text-sm hover:bg-geek-soft"><Copy size={16}/>Copiar link</button><button onClick={()=>sharePost(post)} className="flex w-full items-center gap-2 px-4 py-3 text-left text-sm hover:bg-geek-soft"><Share2 size={16}/>Compartilhar</button>{post.author_id!==userId&&<button onClick={()=>reportPost(post.id)} className="flex w-full items-center gap-2 px-4 py-3 text-left text-sm text-red-300 hover:bg-red-500/10"><Flag size={16}/>Denunciar publicação</button>}</div>}</div>{post.post_type==='card'?<GeekCardPost post={post} author={author}/>:<>{post.content&&<p className="whitespace-pre-wrap break-words px-4 pb-4 text-sm leading-6 text-slate-200">{post.content}</p>}{post.image_url&&<div className="flex max-h-[680px] min-h-24 w-full items-center justify-center overflow-hidden bg-black/30"><img src={post.image_url} alt="Publicação" className="block h-auto max-h-[680px] w-auto max-w-full object-contain"/></div>}</>}<div className="flex items-center gap-5 border-t border-geek-line px-4 py-3 text-sm text-slate-400"><button onClick={()=>toggleLike(post.id)} className={`flex items-center gap-2 ${liked.has(post.id)?'text-red-400':''}`}><Heart size={18} fill={liked.has(post.id)?'currentColor':'none'}/>{likes[post.id]||0}</button><button onClick={()=>toggleComments(post.id)} className="flex items-center gap-2"><MessageCircle size={18}/>{commentCounts[post.id]||0} {commentCounts[post.id]===1?'comentário':'comentários'}</button><button onClick={()=>sharePost(post)} className="ml-auto flex items-center gap-2"><Share2 size={17}/><span className="hidden sm:inline">Compartilhar</span></button></div>{openComments.has(post.id)&&<div className="space-y-3 border-t border-geek-line bg-black/10 p-4">{postComments.length===0?<p className="text-xs text-slate-500">Seja o primeiro a comentar.</p>:postComments.map(comment=>{const ca=profiles[comment.author_id];return <div key={comment.id} className="flex gap-2"><div className="h-8 w-8 shrink-0 overflow-hidden rounded-full bg-gradient-to-br from-orange-400 to-purple-600">{ca?.avatar_url&&<img src={ca.avatar_url} alt="" className="h-full w-full object-cover object-center"/>}</div><div className="min-w-0 rounded-xl bg-geek-soft px-3 py-2"><div className="text-xs font-bold">{ca?.display_name||'Geek'}</div><p className="whitespace-pre-wrap break-words text-sm text-slate-300">{comment.content}</p></div></div>})}<div className="flex gap-2 pt-1"><input value={commentDrafts[post.id]||''} onChange={e=>setCommentDrafts(c=>({...c,[post.id]:e.target.value}))} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();void sendComment(post.id)}}} placeholder="Escreva um comentário..." className="min-w-0 flex-1 rounded-xl border border-geek-line bg-geek-soft px-3 py-2 text-sm outline-none focus:border-geek-orange"/><button onClick={()=>sendComment(post.id)} disabled={!(commentDrafts[post.id]||'').trim()} className="rounded-xl bg-geek-orange px-3 disabled:opacity-50" aria-label="Enviar comentário"><Send size={17}/></button></div></div>}</article>})}
 </div>
}
